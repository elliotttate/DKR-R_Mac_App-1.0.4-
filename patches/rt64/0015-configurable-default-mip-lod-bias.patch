diff --git a/src/render/rt64_framebuffer_renderer.cpp b/src/render/rt64_framebuffer_renderer.cpp
index 9fae0d4..639e87e 100644
--- a/src/render/rt64_framebuffer_renderer.cpp
+++ b/src/render/rt64_framebuffer_renderer.cpp
@@ -572,6 +572,7 @@ namespace RT64 {
                 }
                 
                 rasterParams.renderIndex = i;
+                rasterParams.mipLODBias = shaderLibrary->samplerMipLODBias;
                 rasterParams.screenScale = triangles.screenScale;
                 rasterParams.screenOffset = triangles.screenOffset;
                 worker->commandList->setGraphicsPushConstants(0, &rasterParams);
diff --git a/src/render/rt64_shader_library.cpp b/src/render/rt64_shader_library.cpp
index d310c96..33ff1f6 100644
--- a/src/render/rt64_shader_library.cpp
+++ b/src/render/rt64_shader_library.cpp
@@ -179,6 +179,7 @@
 namespace RT64 {
     namespace {
         std::atomic<uint32_t> gDefaultSamplerAnisotropy{16U};
+        std::atomic<float> gDefaultSamplerMipLODBias{0.0f};
     }
 
     void setDefaultSamplerAnisotropy(uint32_t level) {
@@ -190,11 +191,21 @@ namespace RT64 {
         return gDefaultSamplerAnisotropy.load(std::memory_order_acquire);
     }
 
+    void setDefaultSamplerMipLODBias(float bias) {
+        gDefaultSamplerMipLODBias.store(std::clamp(bias, -2.0f, 2.0f),
+                                        std::memory_order_release);
+    }
+
+    float getDefaultSamplerMipLODBias() {
+        return gDefaultSamplerMipLODBias.load(std::memory_order_acquire);
+    }
+
     // ShaderLibrary
 
     ShaderLibrary::ShaderLibrary(bool usesHDR, bool usesHardwareResolve) {
         this->usesHDR = usesHDR;
         this->usesHardwareResolve = usesHardwareResolve;
+        this->samplerMipLODBias = getDefaultSamplerMipLODBias();
     }
 
     ShaderLibrary::~ShaderLibrary() { }
@@ -221,8 +232,8 @@ namespace RT64 {
             samplerDesc.magFilter = filter;
             samplerDesc.addressW = RenderTextureAddressMode::CLAMP;
 
-            // This matches the bias used in the shader when native samplers are not used.
-            samplerDesc.mipLODBias = -0.25f;
+            // Keep native sampler selection aligned with the manual mip path.
+            samplerDesc.mipLODBias = samplerMipLODBias;
 
             // WRAP.
             samplerDesc.addressU = RenderTextureAddressMode::WRAP;
diff --git a/src/render/rt64_shader_library.h b/src/render/rt64_shader_library.h
index 570b0a4..1dda349 100644
--- a/src/render/rt64_shader_library.h
+++ b/src/render/rt64_shader_library.h
@@ -9,6 +9,8 @@
 namespace RT64 {
     void setDefaultSamplerAnisotropy(uint32_t level);
     uint32_t getDefaultSamplerAnisotropy();
+    void setDefaultSamplerMipLODBias(float bias);
+    float getDefaultSamplerMipLODBias();
 
     struct ShaderRecord {
         std::unique_ptr<RenderPipeline> pipeline;
@@ -19,6 +21,7 @@ namespace RT64 {
         SamplerLibrary samplerLibrary;
         bool usesHDR = false;
         bool usesHardwareResolve = false;
+        float samplerMipLODBias = 0.0f;
 
         // All shaders.
         ShaderRecord bicubicScaling;
diff --git a/src/shaders/RasterPS.hlsl b/src/shaders/RasterPS.hlsl
index e7895c3..a2aa70c 100644
--- a/src/shaders/RasterPS.hlsl
+++ b/src/shaders/RasterPS.hlsl
@@ -143,7 +143,7 @@ LIBRARY_EXPORT bool RasterPS(const RenderParams rp, float4 vertexPosition, float
         
         const GPUTile gpuTile = GPUTiles[globalTileIndex];
         const float2 textureUV = gpuTileFlagHighRes(gpuTile.flags) ? vertexUV : lowResUV;
-        texVal0 = sampleTexture(otherMode, rp.flags, textureUV, ddxVertexUV, ddyVertexUV, rdpTile, gpuTile, false);
+        texVal0 = sampleTexture(otherMode, rp.flags, textureUV, ddxVertexUV, ddyVertexUV, rdpTile, gpuTile, false, gConstants.mipLODBias);
     }
     
     if (renderFlagUsesTexture1(rp.flags)) {
@@ -159,7 +159,7 @@ LIBRARY_EXPORT bool RasterPS(const RenderParams rp, float4 vertexPosition, float
         const GPUTile gpuTile = GPUTiles[globalTileIndex];
         const float2 textureUV = gpuTileFlagHighRes(gpuTile.flags) ? vertexUV : lowResUV;
         const uint nativeSampler = renderFlagNativeSampler1(rp.flags);
-        texVal1 = sampleTexture(otherMode, rp.flags, textureUV, ddxVertexUV, ddyVertexUV, rdpTile, gpuTile, oneCycleHardwareBug);
+        texVal1 = sampleTexture(otherMode, rp.flags, textureUV, ddxVertexUV, ddyVertexUV, rdpTile, gpuTile, oneCycleHardwareBug, gConstants.mipLODBias);
     }
     
     // Color combiner.
diff --git a/src/shaders/TextureSampler.hlsli b/src/shaders/TextureSampler.hlsli
index 1ceb2ce..ab21edb 100644
--- a/src/shaders/TextureSampler.hlsli
+++ b/src/shaders/TextureSampler.hlsli
@@ -214,7 +214,7 @@ float4 sampleTextureLevel(const RDPTile rdpTile, const GPUTile gpuTile, bool fil
     }
 }
 
-float4 sampleTexture(OtherMode otherMode, RenderFlags renderFlags, float2 inputUV, float2 ddxUV, float2 ddyUV, const RDPTile rdpTile, const GPUTile gpuTile, bool nextPixelBug) {
+float4 sampleTexture(OtherMode otherMode, RenderFlags renderFlags, float2 inputUV, float2 ddxUV, float2 ddyUV, const RDPTile rdpTile, const GPUTile gpuTile, bool nextPixelBug, float mipLODBias) {
     const bool texturePerspective = (otherMode.textPersp() == G_TP_PERSP);
     const bool applyCorrection = (!texturePerspective && !renderFlagRect(renderFlags));
 
@@ -278,11 +278,10 @@ float4 sampleTexture(OtherMode otherMode, RenderFlags renderFlags, float2 inputU
             float2 ddxUVScaled = ddxUV * gpuTile.tcScale;
             float2 ddyUVScaled = ddyUV * gpuTile.tcScale;
             float ddMax = max(dot(ddxUVScaled, ddxUVScaled), dot(ddyUVScaled, ddyUVScaled));
-            float mipBias = -0.25f;
-            mip = 0.5 * log2(ddMax) + mipBias;
             float maxMip = float(gpuTile.textureDimensions.z - 1);
-            RDPMipLevels[0] = min(floor(mip), maxMip);
-            RDPMipLevels[1] = min(floor(mip) + 1, maxMip);
+            mip = clamp(0.5 * log2(ddMax) + mipLODBias, 0.0f, maxMip);
+            RDPMipLevels[0] = uint(floor(mip));
+            RDPMipLevels[1] = min(RDPMipLevels[0] + 1U, uint(maxMip));
             numRDPSamples = 2;
         }
         else {
diff --git a/src/shared/rt64_raster_params.h b/src/shared/rt64_raster_params.h
index 0a5c332..39c8590 100644
--- a/src/shared/rt64_raster_params.h
+++ b/src/shared/rt64_raster_params.h
@@ -11,7 +11,8 @@ namespace interop {
 #endif
     struct RasterParams {
         uint renderIndex;
-        uint3 padding;
+        float mipLODBias;
+        uint2 padding;
         float2 screenScale;
         float2 screenOffset;
     };
