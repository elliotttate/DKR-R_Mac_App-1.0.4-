diff --git a/ultramodern/include/ultramodern/ultramodern.hpp b/ultramodern/include/ultramodern/ultramodern.hpp
index e0bd234..8b81c94 100644
--- a/ultramodern/include/ultramodern/ultramodern.hpp
+++ b/ultramodern/include/ultramodern/ultramodern.hpp
@@ -128,10 +128,12 @@ uint32_t get_remaining_audio_bytes();
 struct audio_callbacks_t {
     using queue_samples_t = void(int16_t*, size_t);
     using get_samples_remaining_t = size_t();
     using set_frequency_t = void(uint32_t);
+    using external_work_allowed_t = bool(void);
     queue_samples_t* queue_samples;
     get_samples_remaining_t* get_frames_remaining;
     set_frequency_t* set_frequency;
+    external_work_allowed_t* external_work_allowed;
 };
 
 // TODO: Most of the members of this struct are not used by ultramodern. Should we move them to librecomp instead?
diff --git a/ultramodern/src/audio.cpp b/ultramodern/src/audio.cpp
index af39c40..5d16b19 100644
--- a/ultramodern/src/audio.cpp
+++ b/ultramodern/src/audio.cpp
@@ -13,7 +13,10 @@ void ultramodern::init_audio() {
 }
 
 void ultramodern::set_audio_frequency(uint32_t freq) {
-    if (audio_callbacks.set_frequency) {
+    const bool external_work_allowed =
+        audio_callbacks.external_work_allowed == nullptr ||
+        audio_callbacks.external_work_allowed();
+    if (external_work_allowed && audio_callbacks.set_frequency) {
         audio_callbacks.set_frequency(freq);
     }
     sample_rate = freq;
@@ -30,7 +33,10 @@ void ultramodern::queue_audio_buffer(RDRAM_ARG PTR(int16_t) audio_data_, uint32_t
     uint32_t sample_count = byte_count / sizeof(int16_t);
 
     // Queue the swapped audio data.
-    if (sample_count > 0 && audio_callbacks.queue_samples) {
+    const bool external_work_allowed =
+        audio_callbacks.external_work_allowed == nullptr ||
+        audio_callbacks.external_work_allowed();
+    if (external_work_allowed && sample_count > 0 && audio_callbacks.queue_samples) {
         audio_callbacks.queue_samples(TO_PTR(int16_t, audio_data_), sample_count);
     }
 }
@@ -45,6 +51,13 @@ float buffer_offset_frames = 0.5f;
 // Reporting a number that's too low can lead to audio lag in some games.
 uint32_t ultramodern::get_remaining_audio_bytes() {
+    const bool external_work_allowed =
+        audio_callbacks.external_work_allowed == nullptr ||
+        audio_callbacks.external_work_allowed();
+    if (!external_work_allowed) {
+        return 0;
+    }
+
     // Get the number of remaining buffered audio bytes.
     uint32_t buffered_byte_count;
     if (audio_callbacks.get_frames_remaining != nullptr) {
         buffered_byte_count = audio_callbacks.get_frames_remaining() * 2 * sizeof(int16_t);
diff --git a/ultramodern/src/input.cpp b/ultramodern/src/input.cpp
index 0184c34..8fda5af 100644
--- a/ultramodern/src/input.cpp
+++ b/ultramodern/src/input.cpp
@@ -215,7 +215,10 @@ s32 __osMotorAccess(RDRAM_ARG PTR(OSPfs) pfs_, s32 flag) {
         return PFS_ERR_INVALID;
     }
 
-    if (input_callbacks.set_rumble != nullptr) {
+    const bool physical_output_allowed =
+        input_callbacks.physical_poll_allowed == nullptr ||
+        input_callbacks.physical_poll_allowed();
+    if (physical_output_allowed && input_callbacks.set_rumble != nullptr) {
         input_callbacks.set_rumble(pfs->channel, flag);
     }
 
